---
applications:
  - name: tmo
    instances: 1
    memory: 1G
    timeout: 180
    services:
      - tmo-psql
    env:
      DJANGO_SETTINGS_MODULE: tmo.django.prod
    command: null
    # buildpacks:
      # - python_buildpack
